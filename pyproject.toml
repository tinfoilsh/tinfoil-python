[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tinfoil"
version = "0.0.1"
description = "Python client for the Tinfoil API"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "openai>=1.63.0",
    "pybindgen>=0.22.0",
    "httpx>=0.28",
]
